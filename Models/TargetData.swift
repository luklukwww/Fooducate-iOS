import Foundation

struct TargetData {
    let ingestion: Double
    let ingested: Double
    let carbs: Int
    let carbsIngested: Int
    let protein: Int
    let proteinIngested: Int
    let fat: Int
    let fatIngested: Int
    let carbsExceededCount: Int
    let fatExceededCount: Int
    let proteinExceededCount: Int
    let totalIngestedExceededCount: Int
} 